import 'package:flutter/material.dart';

class Lickedscreen extends StatelessWidget {
  const Lickedscreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
